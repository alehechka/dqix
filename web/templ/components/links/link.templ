package links

templ Link(text string, href templ.SafeURL, attrs templ.Attributes) {
	<a
		href={ href }
		class="text-blue-600 underline data-[not-found=true]:text-gray-400 dark:text-yellow-300"
		{ attrs... }
	>
		{ text }
	</a>
}
